apiVersion: v1
kind: Service
metadata:
  name: hello
  labels:
    app: django
spec:
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 31000
  selector:
    app: django

